# ============================================================
# datauptwo Kubernetes 部署清单（自动生成）
# 来源: deploy/config.yaml
# 数据库: sqlite
# ============================================================

---
apiVersion: v1
kind: Namespace
metadata:
  name: datauptwo
---
apiVersion: v1
kind: ConfigMap
metadata:
  name: datauptwo-config
  namespace: datauptwo
data:
  app.yaml: |
    base:
      mode: prod
      port: 18421
      version: v1.0.0
      language: zh
      install_dir: ./data
      serve_web: true

    database:
      type: sqlite
      path: ./data/registry.db

    log:
      level: info
      time_zone: Asia/Shanghai
      log_name: datauptwo
      max_backup: 30
      max_size: 100

    session:
      timeout: 7200
      secret: ASkDiCV58Mq41oqTpdtsVznnq9Qq6Q4i

    cors:
      allow_origins:
        - "http://localhost"
        - "http://localhost:8080"
        - "http://localhost:4004"
      allow_methods:
        - GET
        - POST
        - PUT
        - DELETE
        - OPTIONS
      allow_headers:
        - Origin
        - Content-Type
        - Authorization
      max_age: 86400

    jwt:
      secret: M5hIsBJdqgBvY2Bd74gw7eJs46KG1Kfx

---
apiVersion: v1
kind: Secret
metadata:
  name: datauptwo-secret
  namespace: datauptwo
type: Opaque
stringData:
  DB_PASSWORD: 
  SESSION_SECRET: ASkDiCV58Mq41oqTpdtsVznnq9Qq6Q4i
  JWT_SECRET: M5hIsBJdqgBvY2Bd74gw7eJs46KG1Kfx

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: datauptwo-data-pvc
  namespace: datauptwo
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: datauptwo
  namespace: datauptwo
spec:
  replicas: 2
  selector:
    matchLabels:
      app: datauptwo
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: datauptwo
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        runAsGroup: 1000
        fsGroup: 1000
      affinity:
        podAntiAffinity:
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 100
              podAffinityTerm:
                labelSelector:
                  matchExpressions:
                    - key: app
                      operator: In
                      values:
                        - datauptwo
                topologyKey: kubernetes.io/hostname
      containers:
        - name: app
          image: registry.example.com/datauptwo:v1.0.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 18421
          env:
            - name: TZ
              value: Asia/Shanghai
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: datauptwo-secret
                  key: DB_PASSWORD
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: datauptwo-secret
                  key: SESSION_SECRET
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: datauptwo-secret
                  key: JWT_SECRET
          volumeMounts:
            - name: config
              mountPath: /app/conf/app.yaml
              subPath: app.yaml
              readOnly: true
            - name: data
              mountPath: /app/data
            - name: logs
              mountPath: /app/logs
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 15
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
      volumes:
        - name: config
          configMap:
            name: datauptwo-config
        - name: data
          persistentVolumeClaim:
            claimName: datauptwo-data-pvc
        - name: logs
          emptyDir: {}
      terminationGracePeriodSeconds: 30

---
apiVersion: v1
kind: Service
metadata:
  name: datauptwo-svc
  namespace: datauptwo
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 18421
      targetPort: http
  selector:
    app: datauptwo

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: datauptwo-ingress
  namespace: datauptwo
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "500m"
spec:
  ingressClassName: nginx
  rules:
    - host: datauptwo.example.com
      http:
        paths:
          - path: /swagger
            pathType: Prefix
            backend:
              service:
                name: datauptwo-svc
                port:
                  number: 18421
          - path: /api
            pathType: Prefix
            backend:
              service:
                name: datauptwo-svc
                port:
                  number: 18421
          - path: /public
            pathType: Prefix
            backend:
              service:
                name: datauptwo-svc
                port:
                  number: 18421
          - path: /
            pathType: Prefix
            backend:
              service:
                name: datauptwo-svc
                port:
                  number: 18421
